
# services:
#   api:
#     build: .
#     container_name: whoslive-api
#     restart: always
#     ports:
#       - "3000:3000"
#     env_file: .env
#     depends_on:
#       - mongodb
#       - redis

#   mongodb:
#     image: mongo:6
#     container_name: whoslive-mongo
#     restart: always
#     ports:
#       - "27017:27017"
#     volumes:
#        - .:/mini-api                   # Mount current directory
#        - /mini-api/node_modules  
#        - mongo_data:/data/db

#   redis:
#     image: redis:7
#     container_name: whoslive-redis
#     restart: always
#     ports:
#       - "6379:6379"
#     command: ["redis-server", "--appendonly", "yes"]
# volumes:
#   mongo_data:


services:
  api:
    build: .
    container_name: whoslive-api
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - .:/app                      # Mount source code for hot reload
      - /app/node_modules           # Preserve node_modules in container
      - /app/dist                   # Preserve dist folder
    environment:
      - NODE_ENV=development        # Change to 'production' for prod
    env_file:
      - ./config/dev.env            # Change to ./config/prod.env for production
    # command: npm run start:dev      # Change to 'npm run start:prod' for production
    command: ["node", "dist/main.js"]
    depends_on:
      - mongodb
      - redis
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:3000/analytics/health"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 5
    #   start_period: 15s
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/ingest/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
#["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/ingest/health"]
  mongodb:
    image: mongo:6
    container_name: whoslive-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db         # Only mount MongoDB data, not app code
    environment:
      - MONGO_INITDB_DATABASE=whoslive

  redis:
    image: redis:7
    container_name: whoslive-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data            # Persist Redis data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  mongo_data:
  redis_data: